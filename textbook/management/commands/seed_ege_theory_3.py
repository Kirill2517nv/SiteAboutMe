"""Разбор задания 3 ЕГЭ: реляционные базы данных в электронных таблицах.

Скелет – docs/ege-theory-brief.md. Отступления от него осознанные:

* блока `code` нет: задание решается в LibreOffice Calc, на экзамене Excel
  не выдаётся, поэтому все формулы даны в синтаксисе LibreOffice – точка
  в ссылке на лист, «;» между аргументами, русские имена функций;
* «Решение» разбито на два шага с картинкой после каждого. Это отдельные
  действия в таблице, а каждый блок статьи – отдельный слайд в режиме показа,
  и оба шага в один слайд не помещаются;
* блок «Если условие другое» здесь намеренный: в банке задания 3 вопрос
  меняется всерьёз (масса или объём, выручка или стоимость, «на сколько
  увеличилось», район вместо улицы), а это одна-две правки в уже написанных
  формулах – ровно тот случай, ради которого блок и заводился;
* «Что нужно уметь в LibreOffice Calc» – виджет screen-steps: восемь навыков,
  у каждого скриншот и абзац. Восемь картинок подряд сделали бы вступление
  длиннее решения. Кадры и записи лежат файлами в media/ege/theory-3/
  (png для скриншотов, mp4 плюс png-постер для видео), пока файла нет –
  виджет рисует аккуратную заглушку;
* image-блоков в статье два – «Журнал с подтянутыми полями» и «Сумма
  выделенного в строке состояния»; картинки им ставит сама команда
  (JOURNAL_IMAGE, SUM_IMAGE), а не админка: файлы лежат в том же каталоге,
  что кадры виджета, и приезжают на прод тем же rsync. Кадр автофильтра
  из «Решения» убран – то же самое, и на той же Прибрежной, показывает запись
  «Автофильтр» в виджете;
* сумма отобранного берётся из строки состояния, а не формулой. Здесь стояла
  `=ПРОМЕЖУТОЧНЫЕ.ИТОГИ(9;…)`, но за ней ученик идёт учить лишнюю функцию,
  тогда как Calc уже написал ответ внизу окна: выделил столбец у отфильтрованных
  строк – рядом с «Выбрано: 17 строк» стоит «Сумма: 1630000». Скрытые фильтром
  строки в эту сумму не входят, и это видно на кадре 09-sum.png.

Все числа статьи проверены пересчётом файла демоверсии в LibreOffice 25.x:
ответ 1630, 17 строк после фильтра, числа ловушек – из «Типичных ошибок».

Статья-заглушка создаётся командой seed_ege_tasks, здесь она наполняется
блоками и публикуется.
"""
from django.conf import settings
from django.core.management.base import BaseCommand
from django.db import transaction

from textbook.models import Article
from textbook.services import replace_blocks
from textbook.templatetags.textbook_tags import markdownify

SLUG = 'ege-3-relyatsionnye-bazy-dannyh'

# Схема базы из условия. Путь относительно MEDIA_ROOT.
SCHEME_IMAGE = 'ege/variant-fipi-demo-2027/images/q03_scheme.png'
SCHEME_SRC = settings.MEDIA_URL + SCHEME_IMAGE

# Файл задания: три листа, 6480 операций в журнале.
DATA_FILE = 'ege/variant-fipi-demo-2027/files/demo_3.ods'
DATA_SRC = settings.MEDIA_URL + DATA_FILE

# Скриншоты слайдов лежат файлами в media, а не в ArticleBlock.image: у блока
# одна картинка, а у виджета их восемь. На прод – rsync media до запуска команды.
SHOTS_DIR = 'ege/theory-3/'

# Кадр к image-блоку «Журнал с подтянутыми полями». Лежит там же и передаётся
# в ArticleBlock.image прямо из команды, а не заливается в админке: файл всё
# равно приезжает на прод вместе с остальными кадрами, а сид, который сам
# ставит картинку, воспроизводится на чистой базе без ручных действий.
JOURNAL_IMAGE = SHOTS_DIR + '08-journal.png'

# Кадр к image-блоку «Сумма выделенного в строке состояния», там же.
SUM_IMAGE = SHOTS_DIR + '09-sum.png'

# Слайды виджета screen-steps: восемь навыков работы в Calc, по абзацу на
# каждый. Тексты – прежний блок «Что нужно уметь в LibreOffice Calc»: жирный
# лид абзаца стал заголовком шага, остальное – текстом.
#
# У шести шагов вместо кадра видео: подсказка аргументов, обводка диапазона,
# заполнение столбца двойным щелчком, заполнение полей мастера функций, отбор
# фильтром существуют только в движении, и на неподвижном кадре от них остаётся
# обрывок. Кадром остались два шага – устройство листов и запятая против точки
# в дробном числе: там нечему происходить, и ролик заставлял бы ждать то, что
# видно сразу.
#
# Общее для всех записей и кадров, снимается один раз и не меняется по ходу:
#   * файл demo_3.ods, интерфейс LibreOffice русский;
#   * окно Calc шириной около 1280 точек, кегль интерфейса крупнее обычного –
#     иначе на проекторе не разобрать;
#   * звука нет, длина 6–12 секунд, мышь ведём медленно: всплывающая подсказка
#     должна успеть появиться и повисеть;
#   * последний кадр держим лишнюю секунду – он же poster, первое, что увидит
#     ученик, и именно он должен отвечать на вопрос шага;
#   * имена файлов – ровно те, что в dict ниже, каталог media/ege/theory-3/.
CALC_STEPS = [
    # Скриншот 01-sheets.png: окно Calc с demo_3.ods целиком, активен лист
    # «Движение_товаров» (видны заголовки полей и первые строки журнала),
    # внизу вкладки всех трёх листов – «Движение_товаров», «Товар», «Магазин».
    dict(
        title='Листы – таблицы базы',
        file='01-sheets.png',
        alt='Вкладки листов внизу окна LibreOffice Calc',
        text=(
            'Файл открывается на листе «Движение_товаров», два других листа – '
            'вкладки внизу окна: «Товар», «Магазин». Строка листа – одна '
            'запись, столбец – поле. Первая строка – заголовки.'
        ),
    ),
    # Видео 02-formula.mp4, 6–8 с. Начало: свободная ячейка L2 справа от
    # журнала – в сам журнал не пишем, чтобы не читалось как «правим базу».
    # Ход: набираем «=СУММ(» – под ячейкой всплывает подсказка аргументов;
    # мышью выделяем E2:E10, диапазон обводится цветной рамкой, а его адрес
    # сам подставляется в формулу; закрываем скобку, Enter.
    # Конец: в ячейке результат, в строке формул «=СУММ(E2:E10)», в поле имени
    # L2 – ровно то, что объясняет абзац: формула живёт в строке формул,
    # в ячейке лежит её результат.
    # В кадре всё время: поле имени, кнопка fx, строка формул, сама ячейка.
    dict(
        title='Формула и строка формул',
        video='02-formula.mp4',
        poster='02-formula.png',
        # Секунды сняты с самой записи (13,5 с): покадровый разбор показал,
        # когда что появляется на экране. Подписывать надо именно смену
        # состояния – иначе ученик видит, что формула меняется сама.
        cues=[
            dict(at=1.0, text='ввожу формулу'),
            dict(at=11.5, text='нажал Enter – в ячейке результат'),
        ],
        alt='Ввод формулы =СУММ(E2:E10): подсказка аргументов, выделение '
            'диапазона мышью, результат в ячейке',
        text=(
            'Формула начинается с `=`. Диапазон не обязательно набирать – '
            'достаточно выделить его мышью, адрес подставится сам. После '
            'Enter в ячейке остаётся результат, а сама формула видна только '
            'в строке формул, когда ячейка выделена.'
        ),
    ),
    # Скриншот 02b-decimal.png: в столбце D числа 3,4 и 4,5 с запятой, под ними
    # формула =D4+D5 даёт 7,9; в столбце F те же числа с точкой – 3.4 и 4.5, и
    # сумма выдаёт #ЗНАЧ!. В кадре строка формул с =D4+D5. Два столбца рядом:
    # одна и та же арифметика, разница только в разделителе.
    dict(
        title='Запятая – десятичный разделитель',
        file='02b-decimal.png',
        alt='Числа с запятой складываются в 7,9, те же числа с точкой дают '
            'ошибку #ЗНАЧ!',
        text=(
            'Дробная часть в русской локали отделяется **запятой**: `3,4` – '
            'это число. Те же цифры с точкой (`3.4`) Calc считает текстом, '
            'сложить их не может и вместо суммы пишет `#ЗНАЧ!`. В задании 3 '
            'на это натыкаются там, где в справочнике дробная цена или '
            'жирность: цифры выглядят как число, а ведут себя как строка.'
        ),
    ),
    # Видео 03-absolute.mp4, 8–10 с. Начало: в G2 набрано
    # «=ВПР(C2;Магазин.A2:C19» – ввод не завершён, курсор внутри ссылки,
    # знаков $ в адресе нет.
    # Запись снята: в кадре формула «=D3» на одну ячейку, и F4 прокручивает её
    # по кругу – D3 → $D$3 → D$3 → $D3 → D3, то есть в том же порядке, что
    # в Excel. Порядок взят из самой записи, а не из документации: в справке
    # LibreOffice за эту смену отвечает Shift+F4, но на рабочей машине круг
    # проходит F4, и именно это видит ученик.
    # Реплик (cues) две, по покадровому разбору файла: подписано нажатие F4
    # и замыкание круга. Комментировать каждую смену $ не надо – ученик читает
    # подпись вместо того, чтобы смотреть на формулу.
    dict(
        title='Абсолютные ссылки: F4',
        video='03-absolute.mp4',
        poster='03-absolute.png',
        cues=[
            dict(at=5.7, text='нажимаю F4 – меняется закрепление'),
            dict(at=10.7, text='ещё F4 – круг замкнулся, ссылка снова без $'),
        ],
        alt='F4 прокручивает ссылку по кругу: D3 → $D$3 → D$3 → $D3 → D3',
        text=(
            'Протягивание формулы вниз сдвигает `C2` в `C3`, `C4`… – это нужно '
            'для ячейки текущей строки. Диапазон справочника сдвигаться не '
            'должен: `$A$2:$C$19`. Знаки `$` расставляет **F4** – та же '
            'клавиша, что в Excel. Поставьте курсор внутрь ссылки и нажимайте, '
            'пока не получится нужный вид: ссылка идёт по кругу `D3` → '
            '`$D$3` → `D$3` → `$D3` и снова `D3`. Знак `$` перед буквой '
            'закрепляет столбец, перед цифрой – строку.'
        ),
    ),
    # Запись снята (12 с): в G3 набирают «=», переходят по вкладке на лист
    # «Магазин», щёлкают по ячейке с адресом – в формуле появляется
    # «=$Магазин.C2», Enter возвращает на журнал.
    # Кадр собран из двух полос исходной записи всего экрана: строка формул
    # с таблицей и полоса с вкладками листов. Иначе главного не видно – что
    # лист сменился, а ввод формулы продолжается.
    dict(
        title='Ссылка на другой лист',
        video='04-sheet-ref.mp4',
        poster='04-sheet-ref.png',
        cues=[
            dict(at=3.0, text='перешёл на лист «Магазин» – ввод формулы '
                              'продолжается'),
            dict(at=5.0, text='щёлкнул по ячейке – в формуле появилось имя '
                              'листа'),
        ],
        alt='Щелчок по вкладке «Магазин» во время ввода формулы подставляет '
            'ссылку $Магазин.C2',
        text=(
            'Ссылка на другой лист пишется через точку: '
            '`$Магазин.$A$2:$C$19`. В Excel та же ссылка – '
            '`Магазин!$A$2:$C$19`, и LibreOffice её не поймёт. Проще всего не '
            'набирать её руками: во время ввода формулы щёлкнуть по вкладке '
            'листа и выделить диапазон мышью – Calc сам подставит правильную '
            'запись.'
        ),
    ),
    # Запись снята (15 с): в G2 собирают формулу «=E2*D2», Enter, курсор
    # на маркер заполнения, двойной щелчок – и заполнены все 6 480 строк
    # (строка состояния внизу кадра это подтверждает: «Выбрано: 6 480 строк»).
    # Нижняя полоса кадра нужна именно ради этой надписи.
    dict(
        title='Протянуть до конца таблицы',
        video='05-fill-handle.mp4',
        poster='05-fill-handle.png',
        cues=[
            dict(at=8.0, text='навёл курсор на маркер заполнения'),
            dict(at=10.6, text='двойной щелчок – формула заполнила все '
                               '6 480 строк'),
        ],
        alt='Двойной щелчок по маркеру заполнения протягивает формулу '
            'на все 6 480 строк журнала',
        text=(
            'Маркер заполнения – квадратик в правом нижнем углу выделения. '
            'Двойной щелчок по нему протягивает формулу до последней '
            'заполненной строки таблицы. В журнале 6480 строк, тащить мышью '
            'их никто не успеет.'
        ),
    ),
    # Слайд «Дата – это число» убран намеренно: в этом решении даты отбирает
    # автофильтр, а не формула, и порядковый номер дня для ответа не нужен –
    # знание верное, но лишнее. Вернётся, если в разборе появится отбор дат
    # формулой (СУММЕСЛИМН со сравнением границ).
    # Запись снята целиком, 39 с: окно открывают кнопкой fx, находят ВПР
    # по имени, заполняют аргументы по очереди – критерий C2, массив выделяют
    # мышью на листе «Магазин» (окно на это время сворачивается в строку),
    # индекс 3, – в окне появляется «Результат: просп. Мира, 45» ещё до «ОК»,
    # и напоследок формулу протягивают на весь столбец. Хвост с протягиванием
    # оставлен намеренно, хотя слайд о нём уже был: здесь ВПР доводится
    # от пустой ячейки до готового столбца, и разрывать это на две записи
    # значит прятать самое нужное – что после «ОК» работа не кончается.
    dict(
        title='Мастер функций',
        video='06-wizard.mp4',
        poster='06-wizard.png',
        cues=[
            dict(at=4.0, text='открыл мастер функций и нашёл ВПР по имени'),
            dict(at=12.0, text='у каждого аргумента своё поле'),
            dict(at=28.0, text='результат виден ещё до нажатия «ОК»'),
            dict(at=35.0, text='и протянул формулу на весь столбец'),
        ],
        alt='Мастер функций: поиск ВПР по имени, заполнение аргументов '
            'по одному, результат до нажатия «ОК» и протягивание столбца',
        text=(
            'Кнопка ƒx слева от строки формул или **Ctrl+F2**. Имя функции '
            'можно не помнить – наберите его часть в поле «Поиск». Дальше '
            'у каждого аргумента своё поле с пояснением, диапазон в поле '
            'можно выделить мышью, а в правом верхнем углу окно считает '
            'результат ещё до нажатия «ОК». Для формулы из разбора это '
            '«просп. Мира, 45» – видно сразу, что ВПР нашла нужный адрес.'
        ),
    ),
    # Слайд про всплывающую подсказку и боковую панель убран: подсказку видно
    # в записи «Формула и строка формул», а второй раз объяснять то же самое
    # незачем. Его место занял автофильтр – инструмент, которым в задании 3
    # делается сам отбор.
    #
    # Запись снята (26 с): включают Данные → Автофильтр, Calc спрашивает,
    # считать ли первую строку заголовком (у служебного столбца заголовка
    # не было) – «Да»; в заголовках появляются стрелки; в списке снимают «Все»
    # и отмечают два адреса на Прибрежной; в журнале остаются только их строки.
    dict(
        title='Автофильтр',
        video='07-filter.mp4',
        poster='07-filter.png',
        cues=[
            dict(at=3.0, text='Calc спрашивает про заголовок столбца – «Да»'),
            dict(at=12.0, text='снял «Все» и отмечаю нужные значения'),
            dict(at=21.0, text='остались только отобранные строки'),
        ],
        alt='Автофильтр: отметки в списке значений столбца и журнал, '
            'в котором остались только отобранные строки',
        text=(
            'Включается один раз: **Данные → Автофильтр** (Ctrl+Shift+L), '
            'и в каждом заголовке появляется стрелка. Если у столбца '
            'заголовка нет, Calc спросит, считать ли им первую строку, – '
            'отвечайте «Да», иначе первая запись журнала уедет в шапку. '
            'В списке под стрелкой снимают «Все» и отмечают нужные значения; '
            'поле поиска в том же списке сокращает его до тех, что содержат '
            'набранное. Условия по разным столбцам складываются: отобрав '
            'поступления, вторым фильтром отбирают товар, и остаются строки, '
            'подходящие сразу под всё. Что фильтр включён, видно по номерам '
            'строк слева – они синие и идут с пропусками, потому что '
            'неподходящие строки скрыты, а не удалены.'
        ),
    ),
]


def _calc_steps():
    """Слайды для screen-steps: Markdown рендерится здесь тем же markdownify,
    что и текст статьи, поэтому виджету не нужен свой разборщик разметки.

    Шаг отдаёт либо `image`, либо `video` с `poster` – смешивать нельзя:
    виджет показывает видео, если ключ есть, и картинка при нём осталась бы
    в конфиге мёртвым весом. Отсюда и проверка ниже: опечатка в ключе выдала бы
    шаг вообще без кадра, а это заметно только глазами на странице.
    """
    steps = []
    for s in CALC_STEPS:
        step = dict(title=s['title'], html=str(markdownify(s['text'])), alt=s['alt'])
        if s.get('video'):
            step['video'] = settings.MEDIA_URL + SHOTS_DIR + s['video']
            step['poster'] = settings.MEDIA_URL + SHOTS_DIR + s['poster']
            if s.get('cues'):
                step['cues'] = s['cues']
        else:
            step['image'] = settings.MEDIA_URL + SHOTS_DIR + s['file']
        steps.append(step)
    assert all(('video' in s) != ('image' in s) for s in steps), 'шаг без кадра'
    return steps


class Command(BaseCommand):
    help = 'Наполняет разбор задания 3 ЕГЭ'

    @transaction.atomic
    def handle(self, *args, **options):
        article = Article.objects.get(slug=SLUG)
        article.description = (
            'Как собрать три листа базы данных в одну таблицу через ВПР, '
            'отфильтровать её и сложить видимые строки – в LibreOffice Calc, '
            'где формулы пишутся иначе, чем в Excel.'
        )

        replace_blocks(article, [
            dict(order=1, block_type='text', title='Что проверяет задание', content=(
                'К заданию прилагается файл электронной таблицы – фрагмент базы '
                'данных. Нужно её проанализировать.\n'
                '\n'
                '**1 балл, норматив 3 минуты.**\n'
                '\n'
                '**Используем LibreOffice Calc в качестве редактора электронных '
                'таблиц.**\n'
                '\n'
                'Что нужно знать заранее:\n'
                '\n'
                '- базовые навыки взаимодействия с LibreOffice Calc;\n'
                '- функцию `ВПР` и абсолютные ссылки со знаком `$`;\n'
                '- автофильтр.'
            )),

            dict(order=2, block_type='text', title='Типовой пример', content=(
                'Задание 3 демонстрационного варианта ЕГЭ-2027 (ФИПИ).\n'
                '\n'
                f'Файл к заданию: [demo_3.ods]({DATA_SRC}) – скачайте его '
                'и повторяйте шаги разбора в LibreOffice.\n'
                '\n'
                'В файле приведён фрагмент базы данных «Кондитерские изделия» '
                'о поставках конфет и печенья в магазины районов города. База '
                'данных состоит из трёх таблиц.\n'
                '\n'
                'Таблица «Движение товаров» содержит записи о поступлении '
                'товаров со склада в магазины в течение июня 2025 г., а также '
                'информацию о проданных товарах. Поле Тип операции содержит '
                'значение Поступление или Продажа, а в соответствующее поле '
                'Количество упаковок, шт. внесена информация о том, сколько '
                'упаковок товара поступило в магазин или было продано в течение '
                'дня. Заголовок таблицы имеет следующий вид.\n'
                '\n'
                '| ID операции | Дата | ID магазина | Артикул | Количество '
                'упаковок, шт. | Тип операции |\n'
                '| --- | --- | --- | --- | --- | --- |\n'
                '\n'
                'Таблица «Товар» содержит информацию об основных характеристиках '
                'каждого товара. Заголовок таблицы имеет следующий вид.\n'
                '\n'
                '| Артикул | Отдел | Наименование товара | Ед_изм | Количество '
                'в упаковке | Цена за упаковку |\n'
                '| --- | --- | --- | --- | --- | --- |\n'
                '\n'
                'Таблица «Магазин» содержит информацию о местонахождении '
                'магазинов. Заголовок таблицы имеет следующий вид.\n'
                '\n'
                '| ID магазина | Район | Адрес |\n'
                '| --- | --- | --- |\n'
                '\n'
                'На рисунке приведена схема указанной базы данных.\n'
                '\n'
                f'![Схема базы данных]({SCHEME_SRC})\n'
                '\n'
                'Используя информацию из приведённой базы данных, определите '
                'общую массу (в кг) всех видов зефира, полученного магазинами, '
                'расположенными на Прибрежной улице, за период с 5 по 19 июня '
                'включительно. В ответе запишите только число.'
            )),

            # Оговорка перед виджетом, а не подписью внутри него: слайдов
            # восемь, и на каждом повторять «формула здесь для примера» –
            # это восемь повторов одной мысли. Оговорка нужна: на записях
            # складывают артикулы и умножают их на количество упаковок, то есть
            # считают величины, которых в задаче нет.
            dict(order=3, block_type='text',
                 title='Восемь приёмов, из которых собирается решение',
                 content=(
                'Дальше – восемь коротких записей: что нажимать и что при этом '
                'происходит на экране. Смотреть их подряд не обязательно, '
                'заголовки над кадром работают как оглавление – можно открыть '
                'тот приём, который нужен сейчас.\n'
                '\n'
                'Формулы на записях **выбраны ради механики, а не ради смысла**: '
                'там складывают артикулы и умножают их на количество упаковок. '
                'Таких величин в задаче нет, и считать их незачем – важно, '
                'как формула вводится, как закрепляется ссылка и как она '
                'расходится по столбцу. Осмысленные формулы этого разбора – '
                'ниже, в блоке «Решение».'
            )),

            # Восемь навыков – виджетом, а не восемью блоками статьи: слайд
            # показывает и текст, и кадр к нему, а вступление не растягивается
            # длиннее самого решения.
            dict(order=4, block_type='widget',
                 title='Что нужно уметь в LibreOffice Calc', content='',
                 widget_key='screen-steps',
                 widget_config={'steps': _calc_steps()}),


            dict(order=5, block_type='text', title='Как рассуждать', content=(
                'Вопрос задания 3 всегда составлен из нескольких независимых '
                'условий, и лежат они на разных листах. Поэтому первое '
                'действие – **разобрать вопрос на условия и для каждого найти '
                'лист**: выписать их столбиком и подписать, откуда брать, '
                'быстрее, чем перечитывать условие по ходу работы.\n'
                '\n'
                '| Условие из вопроса | Поле | Лист |\n'
                '| --- | --- | --- |\n'
                '| «полученного» | Тип операции = Поступление | '
                'Движение_товаров |\n'
                '| «с 5 по 19 июня включительно» | Дата | Движение_товаров |\n'
                '| «на Прибрежной улице» | Адрес | Магазин |\n'
                '| «всех видов зефира» | Наименование товара | Товар |\n'
                '| «массу в кг» | Количество упаковок × Количество '
                'в упаковке | Движение_товаров и Товар |\n'
                '\n'
                'Из таблицы следуют четыре решения.\n'
                '\n'
                '1. **Собирать всё на листе журнала.** Одна строка '
                '«Движение_товаров» – одна операция, а именно операции '
                'и отбираются. Справочники ничего не отбирают, они только '
                'поставляют недостающие поля. Поэтому не фильтровать три листа '
                'по отдельности, а подтянуть адрес, наименование и вес упаковки '
                'в журнал через `ВПР` по ключам: `ID магазина` ведёт '
                'на «Магазин», `Артикул` – на «Товар». Связи видны на схеме.\n'
                '2. **Массы в базе нет – её считаем.** Одна упаковка – это '
                '«Количество в упаковке» в единицах «Ед_изм». У всех четырёх '
                'видов зефира это граммы (250, 800, 500 и 1000 г), значит масса '
                'строки = упаковки × граммы, а ответ – сумма, делённая '
                'на 1000. Проверять столбец «Ед_изм» надо всегда: бывают штуки, '
                'литры, миллилитры.\n'
                '3. **«Всех видов» – это не одно наименование.** Зефира '
                'в справочнике четыре: «Зефир в шоколаде», «Зефир ванильный», '
                '«Зефир воздушный», «Зефир лимонный». Отбирать по началу '
                'названия, а не по одному товару.\n'
                '4. **Прибрежная улица – это два магазина:** M9 («Прибрежная, '
                '7») и M11 («Прибрежная, 21»), оба в Заречном районе. Район '
                'в этом вопросе не нужен – спрашивают улицу.\n'
                '\n'
                'Дальше всё решение – два действия: собрать таблицу, '
                'отфильтровать и сложить.'
            )),

            dict(order=6, block_type='text',
                 title='Решение. Шаг 1: собрать поля в журнал', content=(
                'На листе «Движение_товаров» справа от данных четыре новых '
                'столбца; заголовки в строку 1: Адрес, Товар, В упаковке, '
                'Масса, г. Формулы для строки 2:\n'
                '\n'
                '```\n'
                'G2: =ВПР(C2;$Магазин.$A$2:$C$19;3;0)\n'
                'H2: =ВПР(D2;$Товар.$A$2:$F$61;3;0)\n'
                'I2: =ВПР(D2;$Товар.$A$2:$F$61;5;0)\n'
                'J2: =E2*I2\n'
                '```\n'
                '\n'
                'Аргументы `ВПР` по порядку:\n'
                '\n'
                '- **что ищем** – `C2`, ID магазина из текущей строки;\n'
                '- **где ищем** – `$Магазин.$A$2:$C$19`. Искомое значение '
                'обязано стоять в первом столбце диапазона, а знаки `$` нужны, '
                'чтобы диапазон не уехал при протягивании;\n'
                '- **номер столбца диапазона, откуда взять ответ**: 3 – '
                '«Адрес», столбцы нумеруются с единицы;\n'
                '- **последний аргумент `0`** – точное совпадение. Без него '
                '`ВПР` ищет приблизительно и ошибается (подробно – '
                'в «Типичных ошибках»).\n'
                '\n'
                'Затем выделить G2:J2 и двойным щелчком по маркеру заполнения '
                'протянуть до строки 6481. Проверка первой строки: G2 = '
                '«просп. Мира, 45», H2 = «Батончик соевый», I2 = 250, '
                'J2 = 50000. Если внизу столбцов появилось `#Н/Д` – в диапазоне '
                '`ВПР` забыты `$`.'
            )),

            # Скриншот: лист «Движение_товаров», столбцы A–J, первые ~15 строк.
            # G–J заполнены формулами (в строке формул видна формула ВПР для G2),
            # в строке 1 включён автофильтр (стрелки в заголовках).
            dict(order=7, block_type='image',
                 title='Журнал с подтянутыми полями',
                 image=JOURNAL_IMAGE,
                 content=(
                     'Справа от журнала – четыре подтянутых столбца: адрес '
                     'магазина, наименование, вес упаковки и масса строки. '
                     'Стрелки в заголовках – автофильтр для шага 2.'
                 )),

            dict(order=8, block_type='text',
                 title='Решение. Шаг 2: отфильтровать и сложить', content=(
                'Выделить любую ячейку таблицы → Данные → Автофильтр '
                '(Ctrl+Shift+L). Отбор по четырём столбцам:\n'
                '\n'
                '1. «Тип операции» – оставить только «Поступление».\n'
                '2. «Дата» – оставить с 05.06.2025 по 19.06.2025, обе границы '
                'включительно.\n'
                '3. «Адрес» – в поле поиска выпадающего списка набрать '
                '«Прибреж», останутся два адреса, отметить оба.\n'
                '4. «Товар» – в поле поиска набрать «Зефир», отметить все '
                'четыре.\n'
                '\n'
                'Осталось 17 строк. Складывать их формулой не нужно: выделите '
                'мышью ячейки столбца «Масса, г» от первой отобранной строки '
                'до последней, и Calc напишет сумму сам – внизу окна, в строке '
                'состояния. **Скрытые фильтром строки в неё не попадают**, '
                'хотя выделение и проходит сквозь них: рядом стоит «Выбрано: '
                '17 строк», и это первая проверка, что сложилось именно '
                'отобранное.\n'
                '\n'
                'Сумма – 1630000 граммов, то есть **1630 кг**. '
                'Ответ: **1630**.'
            )),

            # Кадр 09-sum.png снят: отфильтрованный журнал, выделен столбец
            # «Масса, г», в строке состояния обведены «Выбрано: 17 строк»
            # и «Сумма: 1630000». Картинка держит на себе весь шаг 2: без неё
            # «Calc напишет сумму сам» звучит как фигура речи, а строку
            # состояния ученик не разглядывает.
            dict(order=9, block_type='image',
                 title='Сумма выделенного в строке состояния',
                 image=SUM_IMAGE,
                 content=(
                     'Выделен столбец «Масса, г» у отобранных строк. Внизу '
                     'окна: «Выбрано: 17 строк» и «Сумма: 1630000» – это '
                     'граммы, в ответ идут килограммы.'
                 )),

            # Кадра автофильтра здесь нет намеренно: то же самое, и на той же
            # Прибрежной, показывает запись «Автофильтр» в виджете выше.
            dict(order=10, block_type='text',
                 title='Если условие другое', content=(
                'В банке задания 3 база та же по устройству, меняется вопрос – '
                'и каждая перемена стоит одной-двух правок.\n'
                '\n'
                '| Что спрашивают | Что поменять |\n'
                '| --- | --- |\n'
                '| «общий объём (в литрах)» | то же, что масса: упаковки × '
                'количество в упаковке; проверить «Ед_изм» – если миллилитры, '
                'делить на 1000 |\n'
                '| «выручка», «на какую сумму продано» | тип операции '
                '«Продажа», в столбце J – упаковки × «Цена за упаковку» '
                '(6-й столбец «Товар», индекс `ВПР` 6) |\n'
                '| «стоимость полученного» | то же умножение на цену, но тип '
                'операции «Поступление» |\n'
                '| «магазины … района» | подтягивать не «Адрес» (индекс 3), '
                'а «Район» (индекс 2) |\n'
                '| «запишите целую часть» | ответ не округлять, а отбросить '
                'дробную часть |'
            )),

            dict(order=11, block_type='text', title='Типичные ошибки', content=(
                '- **Не отсеяли продажи.** «Полученного» – это только '
                '«Поступление».\n'
                '- **Границы дат взяли строго.** «С 5 по 19 включительно» – '
                'это `>=` и `<=`. Без 5 и 19 июня выходит 1430.\n'
                '- **Ответили не в тех единицах.** Сумма упаковок – 2500, '
                'масса в граммах – 1630000. Спрашивают килограммы.\n'
                '- **`ВПР` без последнего аргумента.** Последний аргумент – '
                'режим поиска, и без него включается приблизительный, '
                'рассчитанный на отсортированный справочник: функция идёт '
                'по первому столбцу и отдаёт последнее значение, которое '
                '**не больше** искомого. ID магазинов – текст, а текст '
                'сравнивается по алфавиту, где `M1` < `M10` < … < `M18` < `M2`, '
                'тогда как в справочнике строки идут по-человечески: M1, M2, '
                'M3… Ища «M11», функция доходит до `M1`, видит, что следующий '
                '`M2` уже больше, и возвращает адрес первого магазина. Так '
                'все магазины с M10 по M18 получают «просп. Мира, 45», '
                '«Прибрежная, 21» из выборки исчезает, и ответ выходит 765. '
                'Ячеек с ошибкой при этом нет ни одной – потому и опасно.\n'
                '- **Забыли `$` в диапазоне `ВПР`.** Диапазон без `$` едет вниз '
                'вместе с формулой: во второй строке журнала это `A2:C19`, '
                'в третьей уже `A3:C20`, а в двадцатой – `A20:C37`, где '
                'справочника нет вовсе. Поэтому с третьей строки из диапазона '
                'выпадает M1 и все его операции дают `#Н/Д`, а ниже двадцатой '
                'строки `#Н/Д` идёт сплошняком.\n'
                '- **Сложили до фильтра или после его снятия.** Сумма в строке '
                'состояния считает то, что выделено сейчас: со снятым фильтром '
                'это все 6 480 строк журнала, а не 17 отобранных. Рядом с '
                'суммой стоит число выделенных строк – сверяйте его.'
            )),
        ])

        article.is_published = True
        article.save(update_fields=['description', 'is_published'])

        self.stdout.write(self.style.SUCCESS(
            'Готово: разбор задания 3 обновлён и опубликован.'))
