# Environment
.env
.env.*

# Python
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Database
db.sqlite3
db_production.sqlite3

# Project-specific
data.json
dump_script.py

# Fixtures (quiz data, keep directory)
fixtures/*
!fixtures/.gitkeep

# Media & static (collected)
media/
staticfiles/

# Spetskurs compiled/WASM static files (deployed manually to server)
static/spetskurs/wasm/*.js
static/spetskurs/wasm/*.wasm
static/spetskurs/wasm/*.html
# Архив исходников библиотеки для школы, где не открывается GitHub (3,6 МБ,
# слепок чужого репозитория вместе с сабмодулями) – тоже выкладывается вручную
static/spetskurs/*.zip

# Библиотека спецкурса – отдельный репозиторий (github.com/Kirill2517nv/GuiLibrary),
# лежит рядом ради сборки .wasm. В дерево сайта не коммитится: у неё свои
# сабмодули glfw/imgui/implot, и `git add .` затянул бы их целиком.
GuiLibrary/

# Node
node_modules/
package-lock.json

# OS
.DS_Store
Thumbs.db
Desktop.ini
nul

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code
.claude/
.mcp.json

# Logs
*.log

# Celery
celerybeat-schedule
celerybeat.pid

# Coverage / testing
htmlcov/
.coverage
.pytest_cache/

# Backup & temp
backups/
*.bak
*.tmp
*.orig

.playwright-mcp

# MkDocs build output
site/

# Вспомогательная кухня: макеты дизайна и одноразовые скрипты отрисовки
design_handoff/
tools/
# Материалы ФИПИ (исходники КИМ, 14 МБ) – в репозиторий не идут
Материалы от ФИПИ для ЕГЭ-2027 по информатике/
